<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:beatTheBank="clr-namespace:BeatTheBank"
             x:DataType="beatTheBank:PlayerListViewModel"
             x:Class="BeatTheBank.PlayerListPage">
    <ContentPage.Content>
        <CollectionView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <!--Player Name - Games Played, Jackpots, Total $ Win, Amount could have won (includes early stops and total losses), -->
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>

<!--
SELECT
    p.Name,
    Games = (SELECT COUNT(*) FROM Games WHERE PlayerId = p.Id),
    AmountWon = (
        SELECT
            SUM(
    )
FROM
    Players p
-->